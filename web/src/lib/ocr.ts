// OCR removed: frontend-only preview mode.
// This stub exists so imports don't break during local development.
export async function performOCR(_: Buffer): Promise<string> {
  throw new Error("OCR functionality removed. This app now runs frontend-only previews.");
}
